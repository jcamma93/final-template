import * as React from 'react';

const Home = ( props: HomeProps) => {
    return (
        <div>
            <h1 className='text-center display-1'>Home Page</h1>
        </div>
    );
};

interface HomeProps {}

export default Home;